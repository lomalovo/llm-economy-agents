{{ bio }}

---

Period {{ step }}.

Cash: {{ money | round(2) }}
Unsold inventory: {{ inventory | round(1) }} units
Market wage: {{ wage | round(2) }} per worker
Current productivity: {{ productivity | round(2) }} units per worker
Last period — consumer spending: {{ total_consumer_demand | round(2) }}, units sold by market: {{ last_sales | round(1) }}
Demand/supply ratio last period: {{ sell_ratio }} (above 1.0 means unsatisfied demand)
Labor market: unemployment {{ (unemployment_rate * 100) | round(1) }}%, vacancy pressure {{ (vacancy_rate * 100) | round(1) }}%
Inflation last period: {{ (inflation_rate * 100) | round(2) }}%
Central bank rate: {{ (interest_rate * 100) | round(2) }}%

{% if history %}
Recent company history:
{% for rec in history %}
  Period {{ rec.step }}: hired={{ rec.hired }}, sold={{ rec.sold }}, stock={{ rec.inventory }}, price={{ rec.price_set }}, balance={{ rec.money | round(2) }}
{% endfor %}
{% endif %}

{% if reflections %}
Your strategic notes from past periods:
{% for r in reflections %}
  [Period {{ r.step }}] {{ r.insight }}
{% endfor %}
{% endif %}

{% if event_notifications %}
This period:
{% for note in event_notifications %}
  {{ note }}
{% endfor %}
{% endif %}

Decide:
- labor_demand: how many workers to hire this period (≥ 0)
- price_setting: price per unit you will charge (> 0)
- production_target: how many units you aim to produce (≥ 0)

In "reasoning", describe your thinking as the person running this company.

Respond in strict JSON.
